import * as React from "react";
import PublicRouter from "./PublicRouter";

const Main: React.SFC = () => (
  <div className="jd-main">
    <div className="jd-side-menu">dd</div>
    <div className="jd-content">
      <PublicRouter />
    </div>
  </div>
);

export default Main;
